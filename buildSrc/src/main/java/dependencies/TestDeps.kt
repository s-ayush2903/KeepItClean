package dependencies

/** Basic Local test Runner Deps **/

object TestDeps {

  const val jupiterApi = "org.junit.jupiter:junit-jupiter-api:${Versions.junitJupiterVersion}"
  const val jupiterParams = "org.junit.jupiter:junit-jupiter-params:${Versions.junitJupiterVersion}"
  const val jupiterEngine = "org.junit.jupiter:junit-jupiter-engine:${Versions.junitJupiterVersion}"
  const val mockk = "io.mockk:mockk:${Versions.mockkVersion}"
  const val junit4 = "junit:junit:${Versions.junit4Version}"

}